// Example of listener code.
// Triggers task when usable disk
// space gets under 100MB

/*
minimalSize = 100 * 1024 * 1024 //100MB
if (new File(".").getUsableSpace() < minimalSize) {
  return true //trigger task
  }
return false //do not trigger task
*/